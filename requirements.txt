# Versiones fijadas a las ya instaladas y verificadas en el equipo de
# desarrollo (Python 3.12, C:\Python312\python.exe). Instalar con:
#   C:\Python312\python.exe -m pip install -r requirements.txt

anthropic==0.120.2

# Google Drive API y Google Sheets API
google-api-python-client==2.198.0
google-auth==2.56.2
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.0

# Configuración y parseo de XML UBL (comprobantes SUNAT)
PyYAML==6.0.3
lxml==6.1.1

# Catálogo de insumos y hojas de cálculo de respaldo
pandas==3.0.3
openpyxl==3.1.5
xlrd==2.0.2

# -----------------------------------------------------------------------------
# Dependencia de desarrollo (no se usa en producción, solo para correr
# tests/test_procesar.py). Instalar junto con las de arriba.
# -----------------------------------------------------------------------------
pytest
